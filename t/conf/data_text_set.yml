# Run this through https://github.com/nigelhorne/App-Test-Generator

our $module   = 'Data::Text;
our $function = 'set';

our $new = '';

our %input = (
	text => { type => 'string' }
);

our %output = (
	type => 'object'
);
